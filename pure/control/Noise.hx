package control;

/**
 * control.Noise:
 * Noise: useful to represent a value with no meaning. No, 'null' doesn't play well
 * with the nice haxe typesystem and Void no longer accepts value members
 * in Haxe 3. Similar to the Noise from tink_core (not use in the project).
 */

 enum Noise { Noise; }
