package control.demo;

/**
 * control.demo.DemoSelect:
 * Unified return type for all demo control objects, representing
 * the next screen to go to.
 */

enum DemoSelect {
    HomeSelect;
    GraphicsSelect;
    InputSelect;
    PhysSelect;
}
