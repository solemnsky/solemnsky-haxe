package solemnsky.tutorial;

import control.Scene;

/**
 * solemnsky.tutorial.TutGraphics:
 * Special graphics functions for the Tutorial.
 */

class TutGraphics {
    public static function renderProp<D>(prop:Prop<D,PropData>) {
        var scene = new Scene();
        return scene;
    }
}
