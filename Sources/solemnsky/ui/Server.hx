package solemnsky.ui;

import solemnsky.core.Core;

/**
 * solemnsky.ui.Server: 
 * Non-graphical server; runs when constructed with a Core.
 * Uses the node.js API.
 */

class Server {
    public function new(core:Core) {
        throw('server not yet implemented');
    }
}